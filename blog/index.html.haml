---
title: 编程改变数据
author: 吴海兴
layout: blog
tab: blog
---
- for post in page.posts
  .blogpost
    %h2
      %a{:href=>post.url}= post.title
    .blogtagbar
      #{post.date.strftime( '%Y-%m-%d' )}
      &nbsp;|&nbsp;
      = page.flattr_compact_counter(:url=>"http://cocasts.com" + post.url, 
                                    :title=>post.title, 
                                    :tags=>post.tags.join(',')) 
      &nbsp;|&nbsp;
      - post.tags.each do |tag|
        %a.label{:href=>tag.primary_page.url}
          = tag
        &nbsp;
    %p
      ~ post.content
    
    %hr
= page.posts.links